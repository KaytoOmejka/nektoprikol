# nekto.me · мост (для пранков)

Локальный сайт, который соединяет двух случайных собеседников из **nekto.me**
друг с другом через бота и пересылает их сообщения. Ты видишь весь диалог в
одном окне и можешь в любой момент написать **от лица любого из них**, чтобы
поймать реакцию.

```
Собеседник 1  ↔  бот(клиент 1)  ↔  [ТЫ]  ↔  бот(клиент 2)  ↔  Собеседник 2
```

> Только для твоего собственного контента. Используются твои токены nekto.me.

## Запуск

```bash
./start.sh
```

или двойной клик по **`start.command`** в Finder (если macOS ругается —
правый клик → «Открыть»).

Скрипт сам создаст виртуальное окружение, поставит зависимости и откроет
интерфейс на <http://127.0.0.1:8765>.

Зависимости: Python 3.10+, `aiohttp`, `python-socketio` (ставятся автоматически).

## Как достать 2 токена nekto.me

Нужны **два** токена — по одному на каждого собеседника (удобно открыть
nekto.me в двух окнах кнопками «Открыть nekto.me» прямо в приложении).

Самый простой способ встроен в приложение: на экране настройки раскрой
**«❓ Как получить токен»** и следуй шагам. Коротко:

**Способ 1 — кнопка-закладка (проще всего, настраивается один раз):**

1. Включи панель закладок браузера: `Ctrl`+`Shift`+`B` (Mac: `⌘`+`Shift`+`B`).
2. Перетащи мышкой кнопку **🔑 Взять токен nekto.me** из приложения на эту панель.
3. Открой nekto.me, начни искать собеседника и **немного попереписывайся**
   (5–10 коротких диалогов) — так аккаунт считается «живым» и бот ищет без капчи.
4. На странице nekto.me нажми закладку **🔑 Взять токен** — токен скопируется
   в буфер. Вставь его в поле «Токен» (`Ctrl`+`V`).
5. Повтори для второго собеседника.

**Способ 2 — через консоль** (если закладка не сработала): на nekto.me нажми
`F12` → вкладка **Console**, вставь и нажми Enter:

```js
copy(JSON.parse(localStorage.getItem("storage_v2")).user.authToken)
```

Токен скопируется в буфер — вставь в приложение.

Поле **User-Agent** заполняется автоматически — менять не нужно.

## Как пользоваться

1. Вставь оба токена → **Соединить**. Статусы пойдут: *подключение → ищет →
   общается*.
2. Реальные реплики обоих появляются в общем окне и автоматически пересылаются
   между собеседниками (тумблер **Автопересылка**).
3. Чтобы вмешаться — выбери **«Писать как: Собеседник 1/2»** (или жми **Tab** в
   поле ввода, чтобы быстро переключить сторону), набери текст и отправь. Второй
   собеседник получит это как реплику первого. В твоём окне такое сообщение
   помечается жёлтым **«вставлено»** (видно только тебе).
4. Если собеседник вышел — сторона подсветится и появится её кнопка **🔄 искать**;
   нового ищем только когда ты сам нажмёшь (автопоиск отключён). **Искать обоим** —
   переподключить сразу обе стороны. **Стоп** — отключиться.

> Каждая вкладка браузера — отдельная независимая сессия со своей парой
> аккаунтов. Можно вести несколько пранков параллельно (нужны свои токены в
> каждой вкладке).

## Аудиочат (голос)

Поставь галочку **«🎙 Аудиочат»** на экране настройки перед «Соединить». Тогда
кроме текста мост будет работать с голосом:

- Ты **слышишь обоих** собеседников (звук воспроизводится в браузере).
- Голос одного собеседника **ретранслируется** другому (как и текст).
- Кнопкой **🎙 Микрофон** в шапке можно в любой момент включить/выключить свой
  микрофон. Когда он включён, твой голос идёт **обоим** собеседникам сразу
  (поверх их голосов) — тебя слышат оба.

### Архитектура

WebRTC целиком в браузере, сервер только **передаёт сигналинг** (SDP/ICE):

```
Незнакомец A ─RTC→ pc[1] ─relay→ pc[2] ─RTC→ Незнакомец B   (+ микрофон оператора)
Незнакомец A ←RTC─ pc[1] ←relay─ pc[2] ←RTC─ Незнакомец B
                         ↕ сигналинг через сервер ↕
```

- `static/audio.js` — два `RTCPeerConnection` (по одному на сторону), релей
  удалённых треков между ними и подмешивание микрофона через Web Audio API,
  воспроизведение обоих голосов. «Perfect negotiation» — устойчивость к glare.
- `server.py` — проксирует сигналинг: `kind=="signal"` → браузеру,
  команда `type=="signal"` → нужному `NektoClient.send_signal`.
- `nekto.py` — **адаптер протокола**: приём notice-сигналинга
  (`_on_signal_notice`) и отправка (`send_signal`).

### ⚠️ Аудиочат: что нужно выяснить (протокол nekto.me)

Протокол голосового чата nekto.me **публично не документирован**. Реализация
выше — рабочий каркас; чтобы он «поехал» на живом сервисе, нужно снять трафик
голосового чата и заполнить плейсхолдеры в `nekto.py` (вверху файла и в методах
`_on_signal_notice` / `send_signal`). Что именно сверить:

1. **Имена событий.** Какими `notice` nekto шлёт SDP-offer/answer и ICE? →
   константа `AUDIO_SIGNAL_NOTICES`. Каким `action` их отправляет клиент? →
   `AUDIO_SIGNAL_ACTION`. (Один общий action или раздельные на offer/candidate?)
2. **Формат полей.** Как лежит SDP (`{type, sdp}`? вложенный `description`?) и
   ICE-кандидат — и нужен ли `dialogId` в исходящем action.
3. **Инициация звонка.** Как начинается аудио-диалог: отдельный режим в
   `search.run` (параметр вида `media:"audio"`?), отдельный action «позвонить»,
   или offer прилетает сразу при `dialog.opened`? Кто инициатор — мы или
   собеседник? (от этого зависит, корректна ли роль `polite` в perfect
   negotiation).
4. **ICE-серверы.** Отдаёт ли nekto свои STUN/TURN (часто — в notice при старте
   звонка)? Их нужно подставить в `ICE` в `static/audio.js` вместо публичного
   STUN.

Снять трафик: открой <https://nekto.me/audiochat>, F12 → **Network → WS** →
выбери соединение `im.nekto.me` → вкладка **Messages/Frames**, начни разговор и
смотри кадры `notice`/`action`. Сырые `notice` уже печатаются и у нас в консоли
(`[nekto raw]`) — по ним видно реальные имена событий. Пришли их — допилю адаптер.

## Файлы

| файл | назначение |
|------|------------|
| `nekto.py` | клиент протокола nekto.me (Socket.IO) |
| `server.py` | сервер: интерфейс + websocket + логика релея |
| `static/` | интерфейс (HTML/CSS/JS) |
| `static/audio.js` | аудиочат: WebRTC, релей голоса, микрофон |
| `start.sh`, `start.command` | запуск |

## Если что-то не работает

- **Не находит собеседника / сразу отключает** — токен «холодный». Попереписывайся
  на nekto.me ещё немного и возьми токен заново.
- **Совсем не подключается** — открой консоль браузера (F12): там в `console.debug`
  печатаются сырые события `[nekto raw]`. По ним видно, если nekto.me поменял
  протокол (например, поле с id пользователя или версию). Пришли их мне — поправлю.
